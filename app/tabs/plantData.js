import React from 'react'
import { Text, View } from 'react-native';
import PropTypes from 'prop-types'

const PlantData = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', marginTop: 30}}>
      <Text> plant data tab </Text>
    </View>
  )
}

export default PlantData;

